query ListPokemon($filter: PokemonFilterInput, $first: Int, $after: String) {
  listPokemon(filter: $filter, first: $first, after: $after) {
    edges {
      cursor
      node {
        id
        name
      }
    }
  }
}
